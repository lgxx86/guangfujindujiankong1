# 依赖
node_modules/

# 构建产物
# dist/        # 已注释:采用「GitHub + 服务器免 build」部署方案,dist/ 需入库(git pull 后直接重启)

# 环境变量（含密钥，禁止上传）
.env

# 系统文件
.DS_Store
Thumbs.db

# IDE
.trae-html-share-packages/
.backend-features.json

# 日志
*.log
npm-debug.log*

# 数据库迁移产物（由 drizzle-kit generate 生成，按需保留）
# db/migrations/

# 临时文件
*.tmp
*.bak
