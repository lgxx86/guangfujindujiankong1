# 大密扣升压站施工进度监控系统 · 腾讯云部署指南

本文档指导您将系统（多人协同版）部署到自购的腾讯云服务器上。全程约需 1~2 小时，按步骤执行即可。

---

## 一、部署架构

```
浏览器（业主/监理/施工方）
        │  HTTP/HTTPS（80/443端口）
        ▼
   Nginx（反向代理，腾讯云服务器）
        │  转发到 127.0.0.1:3000
        ▼
   Node.js 应用（PM2 守护，前后端一体）
        │
        ▼
   MySQL 数据库（腾讯云数据库CDB 或 本机自建MySQL）
```

## 二、准备工作

### 1. 云服务器（CVM）要求

| 项目 | 建议配置 |
|---|---|
| 机型 | 2核4G 起步（项目规模小，2核2G也可用） |
| 操作系统 | **Ubuntu 22.04 LTS**（本指南以此为准） |
| 系统盘 | 50GB 以上 |
| 带宽 | 3~5 Mbps |

### 2. 安全组放行端口

腾讯云控制台 → 云服务器 → 安全组 → 入站规则，放行：

- `22`（SSH，建议限制为您的办公IP）
- `80`（HTTP）
- `443`（HTTPS，如启用证书）
- **不要**对外开放 `3000`（应用端口）和 `3306`（数据库端口）

### 3. 数据库二选一

- **方案A（推荐）**：购买【腾讯云数据库 MySQL】，最省心，自动备份。创建后记录：内网地址、端口、账号、密码，并创建数据库 `dmk_progress`（字符集 utf8mb4）。
- **方案B（省钱）**：在云服务器上自行安装 MySQL，见下文第4步。

---

## 三、服务器环境安装（SSH登录服务器后执行）

### 1. 安装 Node.js 20

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo bash -
sudo apt install -y nodejs
node -v   # 应显示 v20.x.x
```

### 2. 安装 PM2（进程守护）和 Nginx

```bash
sudo npm install -g pm2
sudo apt install -y nginx
```

### 3. （仅方案B）本机安装 MySQL 8

```bash
sudo apt install -y mysql-server
sudo systemctl enable --now mysql

sudo mysql
-- 在 mysql> 提示符下执行：
CREATE DATABASE dmk_progress CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'dmk_user'@'localhost' IDENTIFIED BY '请设置一个强密码';
GRANT ALL PRIVILEGES ON dmk_progress.* TO 'dmk_user'@'localhost';
FLUSH PRIVILEGES;
exit;
```

> 方案A用户：在腾讯云数据库控制台完成同样的建库（数据库名 `dmk_progress`、字符集 utf8mb4）。

---

## 四、上传代码并配置

### 1. 上传代码包

将部署版压缩包 `大密扣升压站进度系统-腾讯云部署版.zip` 上传到服务器（可用 WinSCP / scp / 腾讯云控制台文件上传），例如放到 `/opt`：

```bash
cd /opt
sudo apt install -y unzip
sudo unzip 大密扣升压站进度系统-腾讯云部署版.zip -d dmk-progress
cd dmk-progress
```

### 2. 配置环境变量

```bash
cp .env.example .env
openssl rand -hex 32        # 生成一串随机密钥，复制下来
nano .env
```

修改 `.env` 中三项：

| 配置项 | 填写内容 |
|---|---|
| `DATABASE_URL` | 方案A：`mysql://账号:密码@CDB内网地址:3306/dmk_progress`<br>方案B：`mysql://dmk_user:你的密码@127.0.0.1:3306/dmk_progress` |
| `APP_SECRET` | 上面 `openssl rand -hex 32` 生成的随机串 |
| `ADMIN_PASSWORD` | 管理员初始密码（建议改掉默认值） |

保存：按 `Ctrl+O` 回车，再按 `Ctrl+X` 退出。

### 3. 安装依赖并初始化数据库

```bash
npm install                 # 约3~8分钟
npm run db:push             # 创建数据表（看到 Changes applied 即成功）
npm run db:seed-users       # 创建管理员账号（记下输出的用户名和初始密码）
```

### 4. 构建并启动

```bash
npm run build               # 构建前端+后端，生成 dist/
mkdir -p logs
pm2 start ecosystem.config.cjs
pm2 save                    # 保存进程列表
pm2 startup                 # 按提示再执行一行命令，实现开机自启
```

验证：`curl http://127.0.0.1:3000/api/trpc/ping` 返回 `{"result":...}` 即应用正常。

### 5. 配置 Nginx 反向代理

```bash
sudo cp deploy/nginx.conf /etc/nginx/sites-available/dmk-progress
sudo nano /etc/nginx/sites-available/dmk-progress
# 把 server_name 改为您的域名（没有域名就填服务器公网IP）
sudo ln -s /etc/nginx/sites-available/dmk-progress /etc/nginx/sites-enabled/
sudo rm -f /etc/nginx/sites-enabled/default
sudo nginx -t                 # 显示 successful 才能继续
sudo systemctl reload nginx
```

### 6. 验证访问

浏览器打开 `http://服务器公网IP`（或您的域名），应看到登录页：

- 用 `npm run db:seed-users` 输出的管理员账号登录（默认用户名 `admin`）
- **登录后第一件事：点击右上角钥匙图标修改初始密码**
- 进入「成员管理」→「创建账号」，为监理、施工方人员创建账号并分配角色

---

## 五、启用 HTTPS（可选但推荐）

1. 腾讯云控制台 → SSL 证书 → 申请**免费证书**（需先有域名并解析到服务器IP）
2. 证书签发后下载 Nginx 格式，将 `.pem` / `.key` 上传到 `/etc/nginx/ssl/`
3. 打开 `/etc/nginx/sites-available/dmk-progress`，按文件底部注释启用 443 配置块
4. `sudo nginx -t && sudo systemctl reload nginx`

---

## 六、日常运维命令

```bash
pm2 status                  # 查看应用运行状态
pm2 logs dmk-progress       # 查看应用日志（排查问题首选）
pm2 restart dmk-progress    # 重启应用
sudo systemctl status nginx # 查看Nginx状态
```

**系统更新流程**（以后功能升级后）：

```bash
cd /opt/dmk-progress
# 上传并覆盖新的代码文件后：
npm install
npm run db:push             # 如有表结构变更
npm run build
pm2 restart dmk-progress
```

---

## 七、常见问题排查

| 现象 | 排查方法 |
|---|---|
| 浏览器打不开 | ① 安全组是否放行80端口 ② `sudo systemctl status nginx` ③ `pm2 status` 是否 online |
| 能打开但登录报错 | `pm2 logs dmk-progress` 查看是否数据库连接失败 → 检查 `.env` 的 `DATABASE_URL`；方案A需确认CVM与CDB在同一VPC内网互通 |
| 登录后一直"正在加载" | 数据库表未创建：重新执行 `npm run db:push` |
| `npm run db:push` 报连接拒绝 | 方案B检查 `sudo systemctl status mysql`；方案A检查白名单/内网地址 |
| 忘记管理员密码 | 服务器上执行 `npm run db:seed-users` 不会覆盖；可用管理员在「成员管理」中重置，或联系开发人员操作数据库 |
| 上传照片失败 | Nginx 的 `client_max_body_size` 需 ≥50m（配置文件已含） |

---

## 八、与平台版相比的代码改动清单

以下改动**仅存在于本部署版**，平台版源码（`/mnt/agents/output/app`）未做任何改动：

| 文件 | 改动 |
|---|---|
| `db/schema.ts` | users表增加 `username`、`passwordHash` 字段 |
| `api/local/auth.ts` | **新增**：scrypt密码哈希、本地会话认证 |
| `api/local/router.ts` | **新增**：登录/改密/创建账号/重置密码接口 |
| `api/context.ts` | 会话认证切换为本地认证 |
| `api/boot.ts` | 移除 Kimi OAuth 回调路由 |
| `api/router.ts` | 注册本地认证路由 |
| `api/lib/env.ts` | Kimi 相关环境变量改为可选 |
| `api/lib/cookies.ts` | Cookie 策略适配 HTTP/HTTPS 自有域名部署 |
| `api/kimi/auth.ts`、`api/kimi/platform.ts` | **删除**（Kimi OAuth 死代码） |
| `src/pages/Login.tsx` | 改为账号密码登录页 |
| `src/components/Members.tsx` | 增加创建账号、重置密码功能 |
| `src/pages/Home.tsx` | 增加修改密码入口 |
| `db/seed-users.ts` | **新增**：管理员初始化脚本 |
| `.env.example` | **新增**：环境变量模板 |
| `ecosystem.config.cjs` | **新增**：PM2 配置 |
| `deploy/nginx.conf` | **新增**：Nginx 配置示例 |

> 数据库为全新库，与平台上的演示数据互不影响。如需把平台版已填报的数据迁移过来，请联系开发人员协助导出导入。
