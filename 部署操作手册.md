# 大密扣升压站施工进度监控系统 · 腾讯云部署操作手册

> **文档版本**：v2.0  
> **适用系统**：大密扣升压站进度监控系统（多人协同版）  
> **代码仓库**：https://github.com/lgxx86/guangfujindujiankong1  
> **预估耗时**：1.5 ~ 2.5 小时（含数据库准备）  
> **前置条件**：已购买腾讯云服务器，拥有 GitHub 仓库访问权限

---

## 目录

- [第一章 部署架构与资源规划](#第一章-部署架构与资源规划)
- [第二章 腾讯云资源准备](#第二章-腾讯云资源准备)
- [第三章 服务器环境安装](#第三章-服务器环境安装)
- [第四章 数据库配置（二选一）](#第四章-数据库配置二选一)
- [第五章 系统部署](#第五章-系统部署)
- [第六章 Nginx 反向代理配置](#第六章-nginx-反向代理配置)
- [第七章 HTTPS 证书配置（推荐）](#第七章-https-证书配置推荐)
- [第八章 部署验证与初始化](#第八章-部署验证与初始化)
- [第九章 日常运维命令](#第九章-日常运维命令)
- [第十章 系统更新流程](#第十章-系统更新流程)
- [第十一章 数据备份与恢复](#第十一章-数据备份与恢复)
- [第十二章 常见问题排查](#第十二章-常见问题排查)
- [附录 A 完整配置文件参考](#附录-a-完整配置文件参考)
- [附录 B 端口与安全组速查表](#附录-b-端口与安全组速查表)

---

## 第一章 部署架构与资源规划

### 1.1 系统架构

```
用户浏览器（业主方 / 监理方 / 施工方）
        │
        │  HTTP (80) 或 HTTPS (443)
        ▼
┌───────────────────────────────────────────┐
│         腾讯云服务器 CVM                    │
│                                           │
│   ┌─────────────────────────────────┐     │
│   │  Nginx（反向代理）               │     │
│   │  - 静态文件服务                   │     │
│   │  - API 转发到 127.0.0.1:3000     │     │
│   │  - client_max_body_size 50m      │     │
│   └──────────┬──────────────────────┘     │
│              │                             │
│   ┌──────────▼──────────────────────┐     │
│   │  Node.js 应用（PM2 守护）         │     │
│   │  - 端口: 3000                    │     │
│   │  - 前端静态文件 + tRPC API        │     │
│   │  - 请求体限制: 50MB（照片上传）   │     │
│   └──────────┬──────────────────────┘     │
│              │                             │
│   ┌──────────▼──────────────────────┐     │
│   │  MySQL 8.0                      │     │
│   │  方案A: 腾讯云数据库 CDB（推荐）  │     │
│   │  方案B: 服务器自建 MySQL         │     │
│   └─────────────────────────────────┘     │
└───────────────────────────────────────────┘
```

### 1.2 资源清单

| 资源 | 规格 | 月费用参考 | 说明 |
|------|------|-----------|------|
| 云服务器 CVM | 2核4G, Ubuntu 22.04 | ~60-120元 | 轻量应用服务器也可 |
| 系统盘 | 50GB SSD | 含在 CVM 内 | 存放代码、日志、上传照片 |
| 公网带宽 | 3-5 Mbps | ~20-40元 | 照片上传需要一定带宽 |
| 云数据库 CDB（可选） | MySQL 8.0, 1核1G | ~50-80元 | 方案A推荐，自动备份 |
| 域名（可选） | .com / .cn | ~30-70元/年 | 启用 HTTPS 需要 |
| SSL 证书（可选） | 免费版 | 0元 | 腾讯云提供免费 DV 证书 |

> **最低成本方案**：轻量应用服务器 2核2G + 自建 MySQL，约 50-60元/月

---

## 第二章 腾讯云资源准备

### 2.1 创建云服务器

1. 登录 [腾讯云控制台](https://console.cloud.tencent.com/)
2. 进入 **云服务器 CVM** → **新建实例**
3. 配置选择：

| 配置项 | 推荐值 |
|--------|--------|
| 计费模式 | 按量计费（测试期）/ 包年包月（长期） |
| 地域 | 就近选择（如广州、上海） |
| 机型 | 标准型 SA2 / S5（2核4G） |
| 操作系统 | **Ubuntu Server 22.04 LTS 64位** |
| 系统盘 | 50GB SSD 云硬盘 |
| 登录方式 | 密码（设置一个强密码）或 SSH 密钥 |

4. 创建完成后，记录 **公网 IP 地址**（如 `43.136.xxx.xxx`）

### 2.2 配置安全组

进入 **安全组** → 创建或编辑入站规则：

| 协议 | 端口 | 来源 | 说明 |
|------|------|------|------|
| TCP | 22 | 你的办公 IP /32 | SSH 远程登录（限制来源） |
| TCP | 80 | 0.0.0.0/0 | HTTP 访问 |
| TCP | 443 | 0.0.0.0/0 | HTTPS 访问（如启用） |
| TCP | 3306 | **仅内网** | MySQL（如自建，不对外） |

> **注意**：3000 端口（应用服务）不需要在安全组开放，Nginx 会通过内网转发。

### 2.3 SSH 连接服务器

```bash
# 在本地终端执行
ssh ubuntu@你的服务器公网IP
# 输入创建实例时设置的密码
```

如果使用密钥登录：
```bash
ssh -i /path/to/your-key.pem ubuntu@你的服务器公网IP
```

> **注意**：以下所有命令均在 SSH 登录服务器后执行，以 `ubuntu` 用户身份运行。如遇权限不足，命令前加 `sudo`。

---

## 第三章 服务器环境安装

### 3.1 系统更新

```bash
sudo apt update && sudo apt upgrade -y
```

### 3.2 安装 Node.js 20

```bash
# 添加 NodeSource 官方源
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -

# 安装 Node.js
sudo apt install -y nodejs

# 验证安装
node -v    # 应输出 v20.x.x
npm -v     # 应输出 10.x.x
```

### 3.3 安装 PM2（进程守护）

```bash
sudo npm install -g pm2

# 验证
pm2 -v     # 应输出 5.x.x
```

### 3.4 安装 Nginx（反向代理）

```bash
sudo apt install -y nginx

# 启动并设为开机自启
sudo systemctl enable --now nginx

# 验证
nginx -v   # 应输出 nginx version: nginx/1.x.x
```

### 3.5 安装 Git

```bash
sudo apt install -y git
git --version  # 应输出 git version 2.x.x
```

### 3.6 （可选）安装 MySQL 8.0

> 仅当选择**方案B（服务器自建 MySQL）**时需要执行此步。
> 选择**方案A（腾讯云数据库 CDB）**的用户请跳到 [第四章 方案A](#方案-a腾讯云数据库-cdb推荐)。

```bash
# 安装 MySQL Server
sudo apt install -y mysql-server

# 启动并设为开机自启
sudo systemctl enable --now mysql

# 验证
sudo systemctl status mysql    # 应显示 active (running)
mysql --version                # 应输出 8.0.x
```

---

## 第四章 数据库配置（二选一）

### 方案 A：腾讯云数据库 CDB（推荐）

> 优点：自动备份、高可用、免运维。适合生产环境。

1. **创建数据库实例**
   - 腾讯云控制台 → **云数据库 MySQL** → **新建**
   - 版本：MySQL 8.0
   - 架构：单节点（开发/测试）或主从（生产）
   - 规格：1核1G 起步
   - **VPC**：选择与 CVM 服务器相同的 VPC 和子网（确保内网互通）
   - 密码：设置 root 密码并记录

2. **创建数据库和账号**
   - 进入 CDB 实例 → **数据库管理** → 创建数据库
     - 名称：`dmk_progress`
     - 字符集：`utf8mb4`
     - 排序规则：`utf8mb4_unicode_ci`
   - 进入 **账号管理** → 创建账号
     - 用户名：`dmk_user`
     - 密码：设置一个强密码（如 `Dmk@Pr0d2026!`）并记录
     - 授权数据库：`dmk_progress`（读写权限）

3. **记录连接信息**

   | 参数 | 示例值 |
   |------|--------|
   | 内网地址 | `10.0.0.5` |
   | 端口 | `3306` |
   | 用户名 | `dmk_user` |
   | 密码 | `Dmk@Pr0d2026!` |
   | 数据库名 | `dmk_progress` |

4. **验证连通性**（在 CVM 服务器上执行）

   ```bash
   # 安装 MySQL 客户端
   sudo apt install -y mysql-client

   # 测试连接（替换为你的 CDB 内网地址和密码）
   mysql -h 10.0.0.5 -u dmk_user -p -e "SELECT 1"
   # 输入密码后应显示 +----------+ | 1 |
   ```

   > 如果连接失败：检查 CVM 和 CDB 是否在同一 VPC，以及 CDB 的安全组是否放行了 3306 端口（来源设为 CVM 的内网 IP）。

### 方案 B：服务器自建 MySQL

> 优点：成本更低。适合测试或小规模使用。

```bash
# 登录 MySQL
sudo mysql

# 在 mysql> 提示符下执行以下 SQL：
```

```sql
-- 创建数据库
CREATE DATABASE dmk_progress CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建专用用户（请将密码改为你自己的强密码）
CREATE USER 'dmk_user'@'localhost' IDENTIFIED BY 'Dmk@Pr0d2026!';

-- 授权
GRANT ALL PRIVILEGES ON dmk_progress.* TO 'dmk_user'@'localhost';

-- 刷新权限
FLUSH PRIVILEGES;

-- 退出
EXIT;
```

验证连接：
```bash
mysql -u dmk_user -p -e "USE dmk_progress; SHOW TABLES;"
# 输入密码后应显示 Empty set（空数据库，表还没创建）
```

---

## 第五章 系统部署

### 5.1 克隆代码

```bash
# 创建项目目录
sudo mkdir -p /opt/dmk-progress
sudo chown ubuntu:ubuntu /opt/dmk-progress
cd /opt/dmk-progress

# 从 GitHub 克隆代码
git clone https://github.com/lgxx86/guangfujindujiankong1.git .
```

> **注意**：命令末尾的 `.` 表示克隆到当前目录。如果仓库是私有的，需要输入 GitHub 用户名和 Personal Access Token。

### 5.2 安装依赖

```bash
cd /opt/dmk-progress
npm install
```

> 预计耗时 3~8 分钟。如果网络较慢，可使用国内镜像：
> ```bash
> npm install --registry=https://registry.npmmirror.com
> ```

### 5.3 配置环境变量

```bash
# 从模板创建 .env 文件
cp .env.example .env

# 生成 APP_SECRET（会话签名密钥，记录输出值）
openssl rand -hex 32
# 输出示例：a3f5b8c1d2e3f4a5b6c7d8e9f0a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9
```

编辑 `.env` 文件：

```bash
nano .env
```

填入以下内容（根据你的数据库方案选择）：

```ini
# ===== 数据库连接（必填）=====

# 方案 A（CDB）：替换为你的 CDB 内网地址和密码
DATABASE_URL=mysql://dmk_user:Dmk%40Pr0d2026!@10.0.0.5:3306/dmk_progress

# 方案 B（自建）：
# DATABASE_URL=mysql://dmk_user:Dmk%40Pr0d2026!@127.0.0.1:3306/dmk_progress

# 注意：密码中的特殊字符需要 URL 编码
#   @ → %40    # → %23    ! → %21
#   示例：Dmk@Pr0d2026! → Dmk%40Pr0d2026%21

# ===== 会话密钥（必填）=====
# 粘贴上面 openssl rand -hex 32 的输出
APP_SECRET=a3f5b8c1d2e3f4a5b6c7d8e9f0a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9

# ===== 服务端口 =====
PORT=3000

# ===== 初始管理员账号（仅首次 db:seed-users 时生效）=====
ADMIN_USERNAME=admin
ADMIN_PASSWORD=Dmk@2026admin
ADMIN_NAME=系统管理员
```

保存退出：按 `Ctrl+O` 然后回车，再按 `Ctrl+X`。

> **安全提示**：
> - `.env` 文件包含敏感信息，已在 `.gitignore` 中排除，不会上传到 Git
> - `ADMIN_PASSWORD` 建议改为自定义的强密码
> - 部署完成后请立即在系统中修改管理员密码

### 5.4 初始化数据库表

```bash
# 创建所有数据表（users / project_members / progress_reports / alert_closures）
npm run db:push
```

> 首次执行会提示确认，输入 `y` 回车。看到 `Changes applied` 即成功。

### 5.5 创建管理员账号

```bash
npm run db:seed-users
```

> 输出示例：
> ```
> 管理员账号已创建：
>   用户名: admin
>   密码: Dmk@2026admin
> ```
> 
> **请妥善保存此信息。** 此命令仅在数据库为空时创建，不会覆盖已有账号。

### 5.6 构建生产包

```bash
npm run build
```

> 此命令执行两步：
> 1. `vite build` — 构建前端静态文件到 `dist/public/`
> 2. `esbuild` — 打包后端到 `dist/boot.js`
>
> 预计耗时 30~60 秒。看到 `built in xxx` 和 `Done` 即成功。

### 5.7 创建日志目录

```bash
mkdir -p logs
```

### 5.8 启动应用

```bash
# 使用 PM2 启动（按 ecosystem.config.cjs 配置）
pm2 start ecosystem.config.cjs

# 查看运行状态
pm2 status
# 应显示 dmk-progress 状态为 online

# 查看启动日志
pm2 logs dmk-progress --lines 20
# 应看到 "Server running on http://localhost:3000/"
```

### 5.9 设置 PM2 开机自启

```bash
pm2 save                    # 保存当前进程列表
pm2 startup                 # 生成开机自启脚本
# 终端会提示一条以 sudo 开头的命令，复制并执行它
# 示例：sudo env PATH=$PATH:/usr/bin /usr/lib/node_modules/pm2/bin/pm2 startup systemd -u ubuntu --hp /home/ubuntu
```

### 5.10 验证应用运行

```bash
# 测试 API 是否正常响应
curl http://127.0.0.1:3000/api/trpc/ping
# 应返回类似 {"result":{"data":null}} 的 JSON

# 测试前端页面是否正常
curl -s http://127.0.0.1:3000/ | head -5
# 应返回 HTML（包含 <title>大密扣升压站施工进度监控系统</title>）
```

> 如果以上验证失败，执行 `pm2 logs dmk-progress --lines 50` 查看错误日志。

---

## 第六章 Nginx 反向代理配置

### 6.1 创建站点配置

```bash
sudo cp /opt/dmk-progress/deploy/nginx.conf /etc/nginx/sites-available/dmk-progress
```

### 6.2 修改域名

```bash
sudo nano /etc/nginx/sites-available/dmk-progress
```

将第 8 行的 `server_name` 改为你的域名或服务器公网 IP：

```nginx
server {
    listen 80;
    server_name 43.136.xxx.xxx;    # 改为你的公网 IP 或域名
    client_max_body_size 50m;
    # ... 其余不变
}
```

保存退出：`Ctrl+O` 回车，`Ctrl+X`。

### 6.3 启用站点

```bash
# 创建软链接启用站点
sudo ln -sf /etc/nginx/sites-available/dmk-progress /etc/nginx/sites-enabled/

# 删除默认站点（避免冲突）
sudo rm -f /etc/nginx/sites-enabled/default

# 测试 Nginx 配置语法
sudo nginx -t
# 必须显示 "syntax is ok" 和 "test is successful"

# 重新加载 Nginx
sudo systemctl reload nginx
```

### 6.4 验证外部访问

在本地浏览器打开：

```
http://你的服务器公网IP
```

应看到系统登录页面，标题为「大密扣升压站施工进度监控系统」。

> 如果打不开，依次检查：
> 1. 安全组是否放行 80 端口
> 2. `sudo systemctl status nginx` 是否 running
> 3. `pm2 status` 应用是否 online
> 4. `pm2 logs dmk-progress` 是否有报错

---

## 第七章 HTTPS 证书配置（推荐）

> 启用 HTTPS 可保护登录凭据和会话 Cookie 安全，强烈推荐生产环境启用。
> 需要先准备好域名并解析到服务器 IP。

### 7.1 申请免费 SSL 证书

1. 腾讯云控制台 → **SSL 证书** → **申请免费证书**
2. 填写域名（如 `dmk.example.com`）
3. 验证方式选择 DNS 自动验证（推荐）
4. 等待签发（通常几分钟内）

### 7.2 域名解析

腾讯云控制台 → **DNS 解析 DNSPod** → 添加记录：

| 记录类型 | 主机记录 | 记录值 |
|----------|----------|--------|
| A | dmk | 你的服务器公网 IP |

### 7.3 下载并部署证书

1. 证书签发后，在 SSL 证书页面点击 **下载** → 选择 **Nginx** 格式
2. 解压后得到两个文件：`xxx.pem`（证书）和 `xxx.key`（私钥）
3. 上传到服务器：

```bash
# 在服务器创建证书目录
sudo mkdir -p /etc/nginx/ssl

# 上传证书文件（在本地终端执行）
scp dmk.example.com.pem ubuntu@你的服务器IP:/tmp/
scp dmk.example.com.key ubuntu@你的服务器IP:/tmp/

# 在服务器上移动到证书目录
sudo mv /tmp/dmk.example.com.pem /etc/nginx/ssl/
sudo mv /tmp/dmk.example.com.key /etc/nginx/ssl/
sudo chmod 600 /etc/nginx/ssl/dmk.example.com.*
```

### 7.4 修改 Nginx 配置启用 HTTPS

```bash
sudo nano /etc/nginx/sites-available/dmk-progress
```

将文件修改为：

```nginx
# HTTP → HTTPS 重定向
server {
    listen 80;
    server_name dmk.example.com;          # 改为你的域名
    return 301 https://$host$request_uri;
}

# HTTPS 主配置
server {
    listen 443 ssl;
    server_name dmk.example.com;          # 改为你的域名

    ssl_certificate     /etc/nginx/ssl/dmk.example.com.pem;
    ssl_certificate_key /etc/nginx/ssl/dmk.example.com.key;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    client_max_body_size 50m;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }
}
```

### 7.5 应用配置

```bash
sudo nginx -t                          # 验证语法
sudo systemctl reload nginx            # 重新加载
```

### 7.6 更新安全组

在腾讯云安全组中添加 443 端口入站规则（TCP, 0.0.0.0/0）。

### 7.7 验证

浏览器访问 `https://dmk.example.com`，应看到安全锁标志和系统登录页。

> **系统自动适配**：后端会根据 Nginx 传递的 `X-Forwarded-Proto` 头自动判断是否使用 Secure Cookie，无需额外配置。

---

## 第八章 部署验证与初始化

### 8.1 功能验证清单

用管理员账号登录后，逐项验证：

| 序号 | 验证项 | 操作方法 | 预期结果 |
|------|--------|----------|----------|
| 1 | 登录 | 输入 admin / 密码 | 成功进入 Dashboard |
| 2 | Dashboard 加载 | 查看首页 | 显示项目信息、预警看板、各部位进度 |
| 3 | 甘特图 | 点击「甘特图」标签 | 显示任务时间轴、计划/实际进度 |
| 4 | 任务列表 | 点击「任务列表」标签 | 显示所有任务，可点击填报 |
| 5 | 进度填报 | 选一个任务 → 填报进度 | 填报成功（业主/监理直接生效，施工方待审核） |
| 6 | 照片上传 | 填报时上传照片 | 照片压缩后上传成功 |
| 7 | 审核中心 | 创建施工方账号 → 用施工方填报 → 切回管理员审核 | 审核流程正常 |
| 8 | 预警面板 | 点击「预警」标签 | 显示红色/黄色预警列表 |
| 9 | 成员管理 | 点击「成员管理」→ 创建账号 | 可创建用户并分配角色 |
| 10 | 周报生成 | 点击「周报」标签 | 自动生成周进度报告 |
| 11 | 修改密码 | 点击右上角「修改密码」 | 密码修改成功 |

### 8.2 创建项目成员账号

1. 登录管理员账号
2. 进入「成员管理」
3. 为监理方、施工方人员创建账号：
   - 输入姓名、用户名、密码
   - 选择项目角色：`supervisor`（监理）或 `contractor`（施工）
4. 将账号信息通知相关人员

### 8.3 修改管理员密码

1. 点击右上角「修改密码」
2. 输入旧密码和新密码
3. 点击确认

> **安全要求**：部署后必须修改管理员初始密码。

---

## 第九章 日常运维命令

### 9.1 应用管理

```bash
# 查看应用状态
pm2 status

# 查看实时日志（排查问题首选）
pm2 logs dmk-progress

# 查看最近 100 行日志
pm2 logs dmk-progress --lines 100

# 重启应用
pm2 restart dmk-progress

# 停止应用
pm2 stop dmk-progress

# 删除应用（从 PM2 列表移除）
pm2 delete dmk-progress
```

### 9.2 Nginx 管理

```bash
# 查看 Nginx 状态
sudo systemctl status nginx

# 重新加载配置（修改配置后执行）
sudo systemctl reload nginx

# 测试配置语法
sudo nginx -t

# 重启 Nginx
sudo systemctl restart nginx
```

### 9.3 数据库管理（方案B 自建）

```bash
# 查看 MySQL 状态
sudo systemctl status mysql

# 登录 MySQL
sudo mysql -u dmk_user -p dmk_progress

# 查看 MySQL 日志
sudo tail -50 /var/log/mysql/error.log
```

### 9.4 系统资源监控

```bash
# 查看 CPU/内存使用
htop
# 或
top

# 查看磁盘使用
df -h

# 查看应用内存占用
pm2 monit

# 查看日志文件大小
ls -lh /opt/dmk-progress/logs/
```

### 9.5 清理旧日志

```bash
# PM2 日志会持续增长，定期清理
pm2 flush dmk-progress

# 或手动清空
> /opt/dmk-progress/logs/out.log
> /opt/dmk-progress/logs/err.log
```

---

## 第十章 系统更新流程

当系统有新版本更新时：

```bash
cd /opt/dmk-progress

# 1. 拉取最新代码
git pull origin main

# 2. 安装新依赖（如 package.json 有变化）
npm install

# 3. 更新数据库表结构（如有 schema 变更）
npm run db:push

# 4. 重新构建
npm run build

# 5. 重启应用
pm2 restart dmk-progress

# 6. 验证
pm2 logs dmk-progress --lines 20
```

> **注意**：更新前建议先备份数据库（见第十一章）。

---

## 第十一章 数据备份与恢复

### 11.1 手动备份

```bash
# 方案 A（CDB）：在腾讯云控制台 → 云数据库 → 实例 → 备份恢复 → 手动备份

# 方案 B（自建）：使用 mysqldump
cd /opt/dmk-progress
mkdir -p backups

# 完整备份
mysqldump -u dmk_user -p dmk_progress > backups/dmk_progress_$(date +%Y%m%d_%H%M%S).sql

# 输入密码后完成备份，验证文件
ls -lh backups/
```

### 11.2 自动备份（方案B）

创建定时备份脚本：

```bash
sudo nano /opt/dmk-progress/backup.sh
```

内容：

```bash
#!/bin/bash
BACKUP_DIR="/opt/dmk-progress/backups"
DB_USER="dmk_user"
DB_PASS="你的密码"
DB_NAME="dmk_progress"

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份并压缩
mysqldump -u $DB_USER -p$DB_PASS $DB_NAME | gzip > "$BACKUP_DIR/dmk_progress_$(date +%Y%m%d).sql.gz"

# 保留最近 30 天的备份
find $BACKUP_DIR -name "*.sql.gz" -mtime +30 -delete

echo "备份完成: $(date)"
```

设置定时任务：

```bash
sudo chmod +x /opt/dmk-progress/backup.sh
sudo crontab -e
# 添加以下行（每天凌晨 3 点执行备份）
0 3 * * * /opt/dmk-progress/backup.sh >> /opt/dmk-progress/logs/backup.log 2>&1
```

### 11.3 数据恢复

```bash
# 方案 B（自建）：恢复备份
cd /opt/dmk-progress
gunzip < backups/dmk_progress_20260810.sql.gz | mysql -u dmk_user -p dmk_progress

# 方案 A（CDB）：在腾讯云控制台 → 云数据库 → 备份恢复 → 选择备份点 → 回档
```

---

## 第十二章 常见问题排查

### 12.1 浏览器无法访问

| 排查步骤 | 命令 | 预期结果 |
|----------|------|----------|
| 1. 安全组是否放行 80 端口 | 腾讯云控制台检查 | 80 端口入站规则存在 |
| 2. Nginx 是否运行 | `sudo systemctl status nginx` | active (running) |
| 3. 应用是否运行 | `pm2 status` | dmk-progress 为 online |
| 4. 应用日志是否有错 | `pm2 logs dmk-progress --lines 50` | 无 ERROR |
| 5. 本地能否访问 | `curl http://127.0.0.1:3000/` | 返回 HTML |
| 6. Nginx 转发是否正常 | `curl http://127.0.0.1:80/` | 返回 HTML |

### 12.2 登录报错 / 一直"正在加载"

```
排查方向：数据库连接问题
```

```bash
# 1. 检查应用日志
pm2 logs dmk-progress --lines 50
# 如果看到 "ECONNREFUSED" 或 "Access denied" → 数据库连接问题

# 2. 检查 .env 配置
cat /opt/dmk-progress/.env | grep DATABASE_URL
# 确认地址、端口、用户名、密码正确

# 3. 检查密码中的特殊字符是否已 URL 编码
#   @ → %40    # → %23    ! → %21

# 4. 测试数据库连接
# 方案 A（CDB）：
mysql -h CDB内网地址 -u dmk_user -p -e "SELECT 1"
# 方案 B（自建）：
mysql -u dmk_user -p -e "SELECT 1"

# 5. 确认数据库表已创建
mysql -u dmk_user -p dmk_progress -e "SHOW TABLES;"
# 应显示 4 张表：users, project_members, progress_reports, alert_closures
# 如果为空 → 重新执行 npm run db:push
```

### 12.3 照片上传失败

```
原因：照片过大或 Nginx 限制
```

```bash
# 1. 检查 Nginx 配置中的 body 限制
grep client_max_body_size /etc/nginx/sites-available/dmk-progress
# 应为 50m，如果不是则修改后 reload

# 2. 检查应用层限制（代码中已设为 50MB）
# 前端会自动压缩照片到 800px / JPEG 0.6 质量，通常 < 500KB

# 3. 如果使用 HTTPS，检查 Nginx 443 配置块是否也有 client_max_body_size 50m
```

### 12.4 PM2 应用异常退出

```bash
# 查看错误日志
pm2 logs dmk-progress --err --lines 50

# 常见原因：
# - 端口 3000 被占用：lsof -i :3000，kill 掉占用进程
# - 内存不足：free -h 查看，考虑升级服务器配置
# - .env 配置错误：检查 DATABASE_URL 和 APP_SECRET

# 手动启动测试（绕过 PM2）
cd /opt/dmk-progress
NODE_ENV=production node dist/boot.js
# 观察控制台输出是否有错误
```

### 12.5 忘记管理员密码

```bash
# 方法一：用 MySQL 直接重置（需要知道管理员用户名，默认 admin）
# 先生成新密码的哈希值（在项目目录执行）
cd /opt/dmk-progress
node -e "
const { scryptSync, randomBytes } = require('crypto');
const salt = randomBytes(16).toString('hex');
const hash = scryptSync('你的新密码', salt, 64).toString('hex');
console.log(salt + ':' + hash);
"
# 复制输出的字符串，然后：
mysql -u dmk_user -p dmk_progress -e "UPDATE users SET passwordHash='复制的字符串' WHERE username='admin';"

# 方法二：重新运行 seed（不会覆盖已有数据）
npm run db:seed-users
```

### 12.6 npm install 很慢

```bash
# 使用国内镜像
npm install --registry=https://registry.npmmirror.com

# 或永久设置镜像
npm config set registry https://registry.npmmirror.com
```

### 12.7 git pull 失败（本地有修改）

```bash
# 如果服务器上 .env 被修改导致冲突
git stash
git pull origin main
git stash pop
```

---

## 附录 A 完整配置文件参考

### A.1 .env 文件模板

```ini
# MySQL 数据库连接串（必填）
# 密码特殊字符需 URL 编码：@ → %40  # → %23  ! → %21
DATABASE_URL=mysql://dmk_user:密码@数据库地址:3306/dmk_progress

# 会话签名密钥（必填）
# 生成命令：openssl rand -hex 32
APP_SECRET=一串64位随机十六进制字符串

# 服务端口
PORT=3000

# 初始管理员账号（仅首次执行 db:seed-users 生效）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=请修改为强密码
ADMIN_NAME=系统管理员
```

### A.2 ecosystem.config.cjs（PM2 配置）

```javascript
module.exports = {
  apps: [{
    name: "dmk-progress",
    script: "dist/boot.js",
    cwd: __dirname,
    env: {
      NODE_ENV: "production",
      PORT: 3000,
    },
    instances: 1,
    autorestart: true,
    max_memory_restart: "512M",
    log_date_format: "YYYY-MM-DD HH:mm:ss",
    out_file: "logs/out.log",
    error_file: "logs/err.log",
  }],
};
```

### A.3 Nginx 配置（HTTP 版）

```nginx
server {
    listen 80;
    server_name your-domain.com;       # 改为域名或公网 IP
    client_max_body_size 50m;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }
}
```

### A.4 Nginx 配置（HTTPS 版）

```nginx
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name your-domain.com;

    ssl_certificate     /etc/nginx/ssl/your-domain.com.pem;
    ssl_certificate_key /etc/nginx/ssl/your-domain.com.key;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    client_max_body_size 50m;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }
}
```

---

## 附录 B 端口与安全组速查表

| 端口 | 协议 | 对外开放 | 用途 | 安全组来源 |
|------|------|----------|------|-----------|
| 22 | TCP | 是 | SSH 远程登录 | 限制为办公 IP |
| 80 | TCP | 是 | HTTP 访问 | 0.0.0.0/0 |
| 443 | TCP | 是 | HTTPS 访问 | 0.0.0.0/0 |
| 3000 | TCP | **否** | Node.js 应用 | 仅 127.0.0.1 |
| 3306 | TCP | **否** | MySQL 数据库 | 仅内网/CVM 内网 IP |

---

## 附录 C 部署检查清单

部署完成后逐项打勾确认：

- [ ] 云服务器已创建，可 SSH 登录
- [ ] 安全组已放行 22 / 80 / 443 端口
- [ ] Node.js 20 已安装（`node -v` 确认）
- [ ] PM2 已安装（`pm2 -v` 确认）
- [ ] Nginx 已安装并运行
- [ ] Git 已安装
- [ ] MySQL 已就绪（CDB 或自建）
- [ ] 数据库 `dmk_progress` 已创建（utf8mb4）
- [ ] 数据库用户 `dmk_user` 已创建并授权
- [ ] 代码已克隆到 `/opt/dmk-progress`
- [ ] `npm install` 执行成功
- [ ] `.env` 文件已配置（DATABASE_URL / APP_SECRET）
- [ ] `npm run db:push` 执行成功
- [ ] `npm run db:seed-users` 执行成功
- [ ] `npm run build` 执行成功
- [ ] PM2 启动成功，状态为 online
- [ ] PM2 开机自启已设置
- [ ] Nginx 反向代理已配置
- [ ] `curl http://127.0.0.1:3000/` 返回正常
- [ ] 浏览器可访问登录页面
- [ ] 管理员账号可登录
- [ ] 管理员密码已修改
- [ ] （可选）HTTPS 证书已配置
- [ ] （可选）自动备份已设置

---

> **技术支持**：如部署过程中遇到问题，请先查看 `pm2 logs dmk-progress` 错误日志，参考第十二章常见问题排查。如无法解决，请提供完整的错误日志截图联系开发人员。
